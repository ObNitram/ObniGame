using UnityEngine;

namespace Main.Character
{
    public class PlayerLookKeys : MonoBehaviour
    {
        //private LookSelector directLook;
/*
        private void Awake()
        {
            directLook = GetComponent<LookSelector>();
        }
        
        private void OnEnable()
        {
            InputManagerSingleton.Instance.Player.Look.Enable();
        }
        private void OnDisable()
        {
            InputManagerSingleton.Instance.Player.Look.Disable();
        }

        
        private void Update()
        {
            //directLook.MustLook(InputManagerSingleton.Instance.Player.Look.ReadValue<Vector2>() != Vector2.zero);
            //directLook.SetLookDirection(InputManagerSingleton.Instance.Player.Look.ReadValue<Vector2>());
        }
*/
        
    }
}
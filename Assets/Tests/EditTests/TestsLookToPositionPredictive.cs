using System;
using NUnit.Framework;
using UnityEngine;

namespace Tests.EditTests
{
    public class TestsLookToPositionPredictive
    {
/*
        [Test]
        public void TestIntersectionLineCircle_Tangent()
        {
            // Test with a circle tangent to the line
            Vector2 A = new Vector2(-1f, 0f);
            Vector2 B = new Vector2(1f, 0f);
            Vector2 center = new Vector2(0f, 1f);
            float radius = 1f;
            Vector2[] result = LookToPositionPredictive.IntersectionLineCircle(A, B, center, radius);
            Assert.AreEqual(new Vector2(0f, 1f), result[0]);
            Assert.AreEqual(new Vector2(0f, 1f), result[1]);
        }

        [Test]
        public void TestIntersectionLineCircle_TwoPoints()
        {

            // Test with a circle that intersects the line at two points
            Vector2 A = new Vector2(-1f, -1f);
            Vector2 B = new Vector2(1f, 1f);
            Vector2 center = new Vector2(0f, 0f);
            float radius = Mathf.Sqrt(2);
            Vector2[] result = LookToPositionPredictive.IntersectionLineCircle(A, B, center, radius);
            Assert.AreEqual(new Vector2(0f, 0f), result[0]);
            Assert.AreEqual(new Vector2(0f, 0f), result[1]);

        }
        
        [Test]
        public void TestIntersectionLineCircle_NotIntersect()
        {
            // Test with a circle that does not intersect the line
            Vector2 A = new Vector2(-1f, 0f);
            Vector2 B = new Vector2(1f, 0f);
            Vector2 center = new Vector2(0f, 2f);
            float radius = 1f;
            Vector2[] result = LookToPositionPredictive.IntersectionLineCircle(A, B, center, radius);
            Assert.AreEqual(Array.Empty<Vector2>(), result);
        }
        */
    }
}